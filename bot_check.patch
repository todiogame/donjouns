diff --git a/server/models/GameState.js b/server/models/GameState.js
index abc... def...
--- a/server/models/GameState.js
+++ b/server/models/GameState.js
@@ -107,6 +107,13 @@ class GameState extends Schema {
             }
             player.dead = true;
             player.hp = 0;
+            
+            // Check if only bots remain after this player leaves
+            if (this.onlyBotsRemain()) {
+                console.log("Only bots remain - ending game");
+                this.endGame();
+                return;
+            }
+            
             if (wasCurrentPlayer && typeof this.passTurn === "function") {
                 this.passTurn();
             }
@@ -118,6 +125,21 @@ class GameState extends Schema {
     }

     setPlayerName(playerId, rawName = "") {
+        const player = this.findPlayerById(playerId);
+        if (!player || typeof rawName !== "string") {
+            return;
+        }
+        const sanitized = rawName.replace(/\s+/g, " ").trim();
+        if (!sanitized) {
+            return;
+        }
+        player.name = sanitized.substring(0, 24);
+    }
+
+    onlyBotsRemain() {
+        // Check if game is in active phase
+        const isGamePhase = typeof this.phase === "string" && this.phase.includes("GAME");
+        if (!isGamePhase) {
+            return false;
+        }
+        // Check if all active players (in dungeon) are bots
+        const playersInDungeon = this.players.filter(p => p.inDungeon());
+        if (playersInDungeon.length === 0) {
+            return false;
+        }
+        return playersInDungeon.every(p => p.isBot);
+    }
+
+    setPlayerName(playerId, rawName = "") {
         const player = this.findPlayerById(playerId);
         if (!player || typeof rawName !== "string") {
             return;
@@ -477,6 +492,12 @@ class GameState extends Schema {
         } else {
             this.endGame();
         }
         this.updateItemsUsability();
+
+        // Check if only bots remain
+        if (foundPlayerInDungeon && this.onlyBotsRemain()) {
+            console.log("Only bots remain - ending game");
+            this.endGame();
+        }
     }

     wantToEscape(playerId) {
